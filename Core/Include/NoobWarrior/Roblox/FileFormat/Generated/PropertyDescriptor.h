/*
 * Copyright (C) 2026 Hattozo
 *
 * This file is part of noobWarrior.
 * Generated by NoobWarrior.RobloxFileFormatGenerator; do not edit manually.
 */
#pragma once

#include <any>
#include <span>
#include <string_view>

namespace NoobWarrior::Roblox {
struct PropertyDescriptor {
    std::string_view Name;
    std::string_view Category;
    std::string_view Type;
    bool CanLoad;
    bool CanSave;
    // The engine's default for this property, or null when none is known. A binary column has to
    // carry a value for every instance of its class, so an instance that lacks the property still
    // needs one written -- and the C++ type's zero is the wrong answer often enough to break a
    // place (CollisionGroup wants "Default", CanQuery wants true, Material's lowest member is
    // 256). Captureless lambdas, so the table stays a constant expression.
    std::any (*Default)();
};

struct ClassDescriptor {
    std::string_view Name;
    std::string_view Superclass;
    std::span<const PropertyDescriptor> Properties;
    bool IsService;
    bool Creatable;
};
} // namespace NoobWarrior::Roblox
